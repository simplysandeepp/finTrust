function App() {
  return (
    <main className="page">
      <section className="card">
        <span className="eyebrow">Vite + TypeScript</span>
        <h1>React app is running in Docker</h1>
        <p>
          Edit <code>react-app/src/App.tsx</code> and the container will hot
          reload on <code>localhost:3001</code>.
        </p>
      </section>
    </main>
  );
}

export default App;
