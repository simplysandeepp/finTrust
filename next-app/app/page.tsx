export default function HomePage() {
  return (
    <main className="page">
      <section className="card">
        <span className="eyebrow">Next.js + TypeScript</span>
        <h1>Next app is running in Docker</h1>
        <p>
          Update <code>next-app/app/page.tsx</code> and the dev server will
          refresh on <code>localhost:3002</code>.
        </p>
      </section>
    </main>
  );
}
